version: 2

ethernets:
  mgmt0:
    match:
      macaddress: "${mgmt_mac}"
    set-name: mgmt0
    dhcp4: true

  support0:
    match:
      macaddress: "${support_mac}"
    set-name: support0
    dhcp4: false
    addresses:
      - ${support_ip}/24
